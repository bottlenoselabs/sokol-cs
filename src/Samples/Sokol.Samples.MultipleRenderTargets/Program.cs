namespace Sokol.Samples.MultipleRenderTargets
{
    internal static class Program
    {
        private static void Main(string[] args)
        {
            using var app = new MultipleRenderTargetsApplication();
            app.Run();
        }
    }
}