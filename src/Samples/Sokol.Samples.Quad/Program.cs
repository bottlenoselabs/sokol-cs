namespace Sokol.Samples.Quad
{
    internal static class Program
    {
        private static void Main(string[] args)
        {
            using var app = new QuadApplication();
            app.Run();
        }
    }
}